# Optional. When unset, the frontend uses same-origin /api/* (Vite proxies to
# 127.0.0.1:8000 during `npm run dev`). Set this to your deployed backend URL
# for a hosted frontend build (e.g. https://my-fuel-router.onrender.com).
VITE_API_BASE_URL=
